services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - sqlite_data:/var/www/html/database
    env_file:
      - .env
    restart: unless-stopped

volumes:
  sqlite_data:
